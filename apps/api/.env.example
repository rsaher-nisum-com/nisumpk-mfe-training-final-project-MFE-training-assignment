PORT=4000
# Comma-separated list of origins allowed to call this API (the gateway + every MFE dev origin).
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3003
